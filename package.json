{
  "name": "ym-notion-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Синхронизация заказов Яндекс Маркета (FBS/Express) на отгрузку с базой данных Notion",
  "scripts": {
    "sync": "node --import tsx src/index.ts",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
